# Flutter / Dart
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
# pubspec.lock IS committed: this is an application (not a package) handling
# banking credentials, so builds must be reproducible and dependency changes
# must be reviewable (supply-chain control). See SECURITY_AUDIT.md S10.

# IDE
.idea/
.vscode/
*.iml

# Android
android/.gradle/
android/local.properties
android/key.properties
*.keystore
*.jks

# Recon artifacts — NEVER commit: contain live session cookies
*.har
recon/

# Generated / reference RD report PDFs — contain real customer PII, NEVER commit
*.pdf

# Supabase CLI local temp
supabase/.temp/

# Secrets — build-time injected, NEVER commit
env.json
env.local.json
*.env

# Internal strategy notes — keep local
prod.md
production-scaling.md
chat-agent.md

# Dashboard secrets
admin/keys.js
